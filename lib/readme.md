* x2go is used to add UI to vagrant linux images.   
   * 1) the xwindow server is using x2go server installed on vagrant guest
   * 2) on local laptop , install x2go client and with configuration you can ssh to the GUI of the vagrant servers
